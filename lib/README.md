# Prebuilt libraries

Drop prebuilt `*.lib` files here — every `*.lib` in this folder is linked as-is
(SDCC manual §3.2.4). The folder is listed as `"lib"` in `libraries` of
sdcc-project.json by default; remove it or add more folders to scan.
